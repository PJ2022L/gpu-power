# Operators Source Placeholder

Operator runners belong here when implementation starts.

Initial target operators:

- GEMM
- FlashMLA
- FlashAttention v3

Implementation paths and commits are configured in `configs/operators/*.yaml`.
